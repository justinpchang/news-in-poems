<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Day</title>
  </head>
  <body>
    {{ content }}

    {% assign previous_day = collections.days | getPreviousCollectionItem: page %}
    {% assign next_day = collections.days | getNextCollectionItem: page %}

    <p>
      {% if previous_day %}
        <a href="{{ previous_day.url }}">&lt;</a>
      {% endif %}
      {% if next_day %}
        <a href="{{ next_day.url }}">&gt;</a>
      {% endif %}
    </p>
  </body>
</html>